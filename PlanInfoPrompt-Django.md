# PlanInfoPrompt

Implement this project specification as a Python Django web application using React components styled with Bootstrap for the UI.

The application should follow standard Django and React best practices and project structure.

All list pages should have server side filtering, paging and sorting capabilities.

The application should be sleek, modern, responsive and ready for mobile.
